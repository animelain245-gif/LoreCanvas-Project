# Gradle
.gradle/
build/
*/build/
gradle-app.setting

# Local SDK path config (machine-specific, regenerated by Android Studio
# or scripts/replit-build.sh — never commit this)
local.properties

# Android SDK downloaded/cached by scripts/replit-build.sh on Replit
.android-sdk/

# Android Studio
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Gradle wrapper jar is generated by scripts/replit-build.sh (via `gradle
# wrapper`) or by Android Studio on first open — not committed, since this
# environment cannot produce the binary jar ahead of time.
gradle/wrapper/gradle-wrapper.jar
gradlew
gradlew.bat

# Kotlin
*.class
